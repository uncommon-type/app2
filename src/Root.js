import React from "react";

const Root = () => {
  return (
    <main className="app">
      <section className="app__section">
        <SearchBar />
      </section>
    </main>
  );
};

export default Root;
